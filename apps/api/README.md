# @mdb/api (skeleton)

NestJS REST API for browsing the catalogue (spec §4). Imports `@mdb/core` +
`@mdb/database`. Implemented in a later milestone — this is a placeholder so the
monorepo layout matches the target architecture.
